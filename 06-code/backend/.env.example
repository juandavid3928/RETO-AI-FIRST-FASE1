# Use DATABASE_URL for local development, or the separated DB_* variables used by Compose.
# Values are intentionally blank and must be supplied by the runtime environment.
DATABASE_URL=
DB_HOST=
DB_PORT=
DB_NAME=
DB_USER=
DB_PASSWORD=
JWT_SECRET=
