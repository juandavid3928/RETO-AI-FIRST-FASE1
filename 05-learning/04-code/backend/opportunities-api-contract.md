# Opportunities API contract — borrador

Estado: pendiente de aprobación durante HU-003.

Ruta candidata:

- `GET /api/v1/opportunities`

Pendiente definir filtros, paginación, DTO normalizado y errores externos. El frontend deberá consumir esta API y nunca SECOP directamente.
